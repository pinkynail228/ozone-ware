# 🚀 Деплой на Vercel

## Метод 1: Через GitHub (рекомендуется)

### Шаг 1: Создать Git репозиторий

```bash
# Инициализировать git (если еще не создан)
git init

# Добавить все файлы
git add .

# Коммит
git commit -m "OzonWare v2.1 - 8 игр готовы"
```

### Шаг 2: Залить на GitHub

```bash
# Создать репозиторий на github.com, потом:
git remote add origin https://github.com/ВАШ_USERNAME/ozonware.git
git branch -M main
git push -u origin main
```

### Шаг 3: Подключить Vercel

1. Зайти на [vercel.com](https://vercel.com)
2. Нажать **"Add New Project"**
3. **"Import Git Repository"**
4. Выбрать репозиторий `ozone-warioware`
5. Настройки:
   - **Framework Preset**: Other
   - **Root Directory**: ./
   - **Build Command**: (оставить пустым)
   - **Output Directory**: ./
6. Нажать **"Deploy"**

### ✅ Готово! 
Vercel автоматически задеплоит проект и даст ссылку типа:
```
https://ozonware.vercel.app
```

---

## Метод 2: Через Vercel CLI (быстрый)

### Установить Vercel CLI:

```bash
npm install -g vercel
```

### Залогиниться:

```bash
vercel login
```

### Задеплоить:

```bash
cd "/Users/starasov/Documents/OzonWare Inc./CascadeProjects/windsurf-project"
vercel
```

**Ответы на вопросы:**
- Set up and deploy? → **Y**
- Which scope? → (выбрать свой аккаунт)
- Link to existing project? → **N**
- What's your project's name? → **ozone-warioware**
- In which directory is your code located? → **./
- Want to override settings? → **N**

### ✅ Готово!
Vercel задеплоит и даст ссылку.

### Повторный деплой (после изменений):

```bash
vercel --prod
```

---

## Метод 3: Через Vercel Web (без CLI)

### Шаг 1: Создать vercel.json

```bash
# Файл уже создан ниже
```

### Шаг 2: Загрузить на Vercel

1. Зайти на [vercel.com](https://vercel.com)
2. Нажать **"Add New Project"**
3. Вкладка **"Deploy"** → перетащить папку проекта
4. Или нажать **"Browse"** и выбрать папку

### ✅ Готово!

---

## 📋 Важные файлы для Vercel:

### vercel.json (опционально)
```json
{
  "version": 2,
  "name": "ozone-warioware",
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
```

### .vercelignore (опционально)
```
node_modules
.DS_Store
*.log
.env
```

---

## 🔧 Настройки проекта на Vercel:

После деплоя можно настроить:

### 1. Кастомный домен
```
Settings → Domains → Add
```

### 2. Environment Variables
```
Settings → Environment Variables
(пока не нужно)
```

### 3. Performance
```
Analytics → Enable
(для отслеживания посетителей)
```

---

## 🧪 Тестирование после деплоя:

1. **Открыть ссылку** от Vercel
2. **Проверить на мобильном**:
   - Открыть на iPhone/Android
   - Проверить все 8 игр
   - Проверить тач-события
3. **Проверить производительность**:
   - Chrome DevTools → Lighthouse
   - Должно быть 90+ Performance

---

## ⚡ Автодеплой (GitHub метод):

При использовании GitHub каждый push автоматически деплоится:

```bash
# Сделать изменения
git add .
git commit -m "Фикс бага"
git push

# Vercel автоматически задеплоит!
```

---

## 🎯 Рекомендуемый процесс:

```bash
# 1. Создать репозиторий на GitHub
# 2. Залить код
git init
git add .
git commit -m "Initial commit - 8 games ready"
git remote add origin https://github.com/USERNAME/ozone-warioware.git
git push -u origin main

# 3. Подключить Vercel к GitHub
# (через vercel.com интерфейс)

# 4. Готово! Каждый push = автодеплой
```

---

## 📱 Поделиться игрой:

После деплоя получишь ссылку типа:
```
https://ozonware-xyz123.vercel.app
```

Можно:
- 📱 Открыть на мобильном
- 🔗 Поделиться в соцсетях
- 📧 Отправить на конкурс Ozone Design
- 🎮 Дать друзьям протестировать

---

## 🐛 Troubleshooting:

### Ошибка: "No index.html found"
```bash
# Убедись что index.html в корне проекта
ls -la index.html
```

### Ошибка: "Build failed"
```bash
# Для статического сайта build не нужен
# В настройках Vercel оставь Build Command пустым
```

### Игра не работает на продакшене
```bash
# Проверь console.log в DevTools
# Проверь пути к файлам (должны быть относительные)
```

---

## ✅ Чек-лист перед деплоем:

- [x] Все 8 игр работают локально
- [x] Баги исправлены
- [x] Console.log можно оставить (или удалить)
- [x] index.html в корне проекта
- [x] Все пути относительные (js/*, css/*)
- [x] .gitignore создан (если через Git)

---

**Готово к деплою! 🚀**

Используй Метод 1 (GitHub) для продакшена или Метод 2 (CLI) для быстрого тестирования.
